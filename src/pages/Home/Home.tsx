import styled from 'styled-components';

const Header = styled.h1`
  font-size: 32px;
  color: antiquewhite;
`;

const Home = () => (
  <div>
    <Header>Home page</Header>
  </div>
);

export default Home;
